# Software Platform

- `backend/` — APIs, services and device management
- `frontend/` — web portals and operator interfaces
- `mobile/` — mobile applications
- `edge/` — local gateway and offline operation
- `database/` — schema and migrations
- `tools/` — engineering and commissioning utilities

Production secrets and credentials are never stored in source control.